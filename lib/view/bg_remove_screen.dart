import 'package:flutter/material.dart';

class BackgroundRemoveScreen extends StatelessWidget {
  const BackgroundRemoveScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}